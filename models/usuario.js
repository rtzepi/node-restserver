// visualizar como queremos ver el modelo de usuario en la base de datos
// Mongo se graba en objetos
// Este modelo servira para hacer las interacciones con la base de datos
// Trabajar en datos en usuarios.js controllers en usuariosPost
// Instalar mongoose npm i mongoose
const { Schema, model } = require('mongoose');

const UsuarioSchema = Schema({
    nombre: {
        type: String,
        required: [true, 'El nombre es obligatorio']
    },
    correo: {
        type: String,
        required: [true, 'El correo es obligatorio'],
        unique: true
    },
    password: {
        type: String,
        required: [true, 'El password es obligatorio'],
    },
    img: {
        type: String,
    },
    rol: {
        type: String,
        required: true,
        enum: ['ADMIN_ROLE', 'USER_ROLE']
    },
    estado: {
        type: Boolean,
        default: true
    },
    google: {
        type: Boolean,
        default: false
    },
});

module.exports = model('Usuario', UsuarioSchema);