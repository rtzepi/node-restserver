//Agregar GET, POS, PUT, DELETE
//Probar con postman http://localhost:8080/api como html y analizar
//Probar con postman http://localhost:8080/ api como json y analizar

require('dotnev').config();

const Server = require('./models/server2');

const server = new Server();